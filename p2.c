#include <stdio.h>

int main() {
    float cont, nota, media, soma;
    for (cont-0; cont<9; cont++) {
        printf("digite a nota: ");
        scanf("%f", &nota);
        soma = soma+nota;
        media= (nota/9);
        }
        if (media<=6.5) {
            printf("Aprovado");
        }
        else {
            printf("Reprovado");
            }
            }
